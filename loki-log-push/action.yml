name: 'pleme-io · loki-log-push'
description: 'Push a batch of log lines to a Loki ingester.'
branding: { icon: 'file-text', color: 'orange' }

inputs:
  url:
    required: true
  labels:
    required: false
    default: '{"app":"ci"}'
  lines-file:
    required: true

outputs:
  pushed:
    value: ${{ steps.run.outputs.pushed }}

runs:
  using: composite
  steps:
    - id: src
      shell: bash
      run: |
        set -euo pipefail
        out="${RUNNER_TEMP}/loki-log-push.tlisp"
        {
          # stdlib read in-checkout, at the SAME ref as the tlisp below -- never fetched.
          cat "${{ github.action_path }}/../_tlisp-stdlib/stdlib.tlisp"
          echo
          cat ${{ github.action_path }}/run.tlisp
        } > "$out"
        echo "script-file=$out" >> "$GITHUB_OUTPUT"
    - id: run
      uses: pleme-io/actions/tatara-script@main
      with:
        script-file: ${{ steps.src.outputs.script-file }}
      env:
        URL_ARG: ${{ inputs.url }}
        LABELS: ${{ inputs.labels }}
        LINES_FILE: ${{ inputs.lines-file }}
