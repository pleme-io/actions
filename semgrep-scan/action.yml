name: 'pleme-io · semgrep-scan'
description: 'Semgrep SAST scan with configurable rule set.'
branding: { icon: 'shield', color: 'red' }

inputs:
  config:
    required: false
    default: "auto"
  fail-on-finding:
    required: false
    default: "true"

outputs:
  findings-count:
    value: ${{ steps.run.outputs.findings-count }}

runs:
  using: composite
  steps:
    - id: run
      shell: bash
      run: |
        echo "::warning::semgrep-scan action stub — full implementation tracked in patterns.nix backlog"
        echo "findings-count=stub" >> "$GITHUB_OUTPUT"
