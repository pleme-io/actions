# Tracked NO-SHELL debt -- action.yml files whose shell body exceeds the
# threshold, predating action-shell-lint actually working (it silently
# crashed on a named-let syntax error until this file's own commit, so
# it never enforced anything and this debt accumulated unseen). Each
# entry here is real, tracked debt, not a silent exception -- the goal
# is an empty file, not a permanently-growing one. Remove an entry only
# by actually fixing that action's shell body (route it through tlisp),
# never by raising the threshold to make it fit.
#
# ── AMENDED 2026-08-01: the list below the original ten is NOT "predating
# the lint". Those are entries added later, and each one states its reason
# inline, the same discipline runtime-install-lint and banned-tool-lint
# already use. Saying so here rather than letting the header above quietly
# misdescribe half the file.
#
# The bar for adding an entry, so this does not become the growing file the
# header warns about: converting the block must be a WORSE engineering act
# than tracking it, and the reason must be specific enough to argue with.
# "It is long" is not a reason. "It is security-critical and cannot be
# exercised" is.
./aldrava-dispatch/action.yml
./caixa-render/action.yml
./email-notify/action.yml
./aldrava-resolve/action.yml
./sui-dockerfile-node-cache/action.yml
./aldrava-lint/action.yml
./tatara-script/action.yml
./caixa-publish/action.yml
./wasm-build/action.yml

# ── Added 2026-08-01, each with its reason ──────────────────────────────
#
# aldrava-install: a pinned, checksum-verified binary installer written the
# same day (e2f4afc) specifically to close a floating-`latest` supply-chain
# gap. Porting it means rewriting a checksum-verification path, and the
# download arm CANNOT BE EXERCISED: pleme-io/aldrava has zero releases, so
# every asset URL 404s (probed; control — tatara-lisp's equivalent URL
# returns 200, so the probe reads GitHub correctly). An unverifiable
# rewrite of verification code is a worse act than tracking it. Convert
# when aldrava cuts its first release and the path can actually be run.
./aldrava-install/action.yml
#
# image-scan: ONE body left, down from two (the third flag was its loader,
# the false positive fixed in run.tlisp today).
#
#   * "Trivy sensitivity canary" — CONVERTED 2026-08-01, exactly the way
#     this entry said it should be: the SPDX fixture moved to a committed
#     file (trivy-canary.spdx.json) FIRST, then the orchestration typed
#     into canary.tlisp with the exit-code semantics as a pure function
#     under unit test (canary.test.tlisp, 5 assertions). ~81 shell lines
#     to zero.
#   * "Install Trivy" — STILL HERE, 79 lines. It pins BOTH the installer
#     script tag and the scanner version, and that dual pin is the only
#     reason a scan verdict is reproducible at all. Security-critical and
#     freshly reviewed; converting it is the remaining work, not a
#     formality.
#
# Kept as one entry because the baseline is file-granular — which is also
# why image-scan was NOT baselined earlier today when only its loader was
# flagged, since that would have hidden both real bodies.
./image-scan/action.yml
