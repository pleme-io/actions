# Tracked runtime-install debt (theory/NIX-HARDENING.md §VI).
#
# 2026-08-01: this file previously read "Empty as of 2026-07-22 -- all 23
# violations found in the initial catalog sweep are purged" and called itself
# "the forcing function that keeps the count at zero". The count was zero only
# because every pattern required a PIPE (`curl … |`), and the dominant real
# shape is unpiped:
#
#     curl -sSL <host>/releases/latest/download/<tool> -o /tmp/x && chmod +x /tmp/x
#
# Twelve actions were doing exactly that, unseen. They are listed here so they
# are TRACKED DEBT reported on every run, not silence. Nothing below is
# accepted; each line is work.
#
# CORRECTION, 2026-08-01, to this file's own first draft. It said "all twelve"
# lacked --fail. Measured per file rather than by eye: TEN lacked it; image-scan
# (`curl -sfL`) and sbom-generate (`curl -sSfL`) always had it. Writing "all
# twelve" was the same round-up this repo keeps correcting elsewhere, committed
# by the very entry that names the habit.
#
# The --fail defect is now FIXED for all ten (`-sSL`->`-fsSL`, `-sL`->`-fsL`),
# so what remains below is two defects, not three:
#   * UNPINNED -- `releases/latest` or a `main` install.sh, so the binary can
#     change under us between two runs of the SAME commit. This is the one that
#     makes a scan verdict irreproducible. NOW FIXED for the three that decide
#     what we ship: image-scan (trivy v0.72.0), sbom-generate (syft v1.50.0)
#     and slsa-attest (witness v0.12.0) pin BOTH the install script and the
#     version. The other nine are still on `releases/latest`.
#
# AND SIX OF THE TWELVE WERE 404-ING (measured 2026-08-01, per URL):
#     slsa-attest      witness-linux-amd64            404  <- asset never existed
#     api-spec-diff    oasdiff_linux_amd64.tar.gz     404
#     nats-publish     nats-0.1.6-linux-amd64.zip     404
#     aldrava-{resolve,dispatch,lint}                 404  <- repo has NO releases
#   200 only for: argocd-sync, cyclonedx-merge, image-scan, sbom-generate,
#   onepassword-fetch, defaction-render.
# With the old `-sSL` these exited 0, wrote GitHub's 404 HTML to the target and
# `chmod +x`'d it, so the fault surfaced later as an exec error in the tool's
# own step. slsa-attest (2 live consumer workflows) and aldrava-resolve /
# aldrava-dispatch (1 each) are LIVE; the other three have 0 consumers.
# slsa-attest is fixed here. aldrava needs a RELEASE, not an action edit.
#   * NOT HERMETIC -- fetched at job time instead of baked into a
#     pleme-io/hardened-images distroless image (§VI.1). That image does not
#     exist yet for trivy/syft/grype -- checked, `grypePkg` is an internal
#     scan package, not a shipped image -- so baking one is the real work
#     these entries represent.
#
# FIXED and no longer true of any entry:
#   * NO --fail -- `-sSL` exited 0 on an HTTP error, wrote the error body to
#     the destination, and `chmod +x` made a 404 page "executable". That was
#     precisely the class run.tlisp's header says this lint closes, and the
#     lint could not see it. Closed for all 12 as of this commit.
#
# Ordered by what it costs us to be wrong, worst first: the top four ARE the
# supply-chain toolchain. An unpinned scanner or attestor makes a verdict we
# cannot reproduce -- "0 CVE" asserted by a binary we cannot name.
./image-scan/action.yml
./sbom-generate/action.yml
./slsa-attest/run.tlisp
./cyclonedx-merge/run.tlisp
./argocd-sync/run.tlisp
./aldrava-dispatch/action.yml
./aldrava-lint/action.yml
./aldrava-resolve/action.yml
./api-spec-diff/run.tlisp
./nats-publish/run.tlisp
./onepassword-fetch/action.yml
./defaction-render/action.yml
