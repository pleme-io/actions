name: 'pleme-io · pinecone-upsert'
description: 'Upsert vectors to Pinecone.'
branding: { icon: 'database', color: 'blue' }

inputs:
  api-key:
    required: true
  index:
    required: true
  vectors-file:
    required: true

outputs:
  upserted:
    value: ${{ steps.run.outputs.upserted }}

runs:
  using: composite
  steps:
    - id: src
      shell: bash
      run: |
        set -euo pipefail
        out="${RUNNER_TEMP}/pinecone-upsert.tlisp"
        {
          # stdlib read in-checkout, at the SAME ref as the tlisp below -- never fetched.
          cat "${{ github.action_path }}/../_tlisp-stdlib/stdlib.tlisp"
          echo
          cat ${{ github.action_path }}/run.tlisp
        } > "$out"
        echo "script-file=$out" >> "$GITHUB_OUTPUT"
    - id: run
      uses: pleme-io/actions/tatara-script@main
      with:
        script-file: ${{ steps.src.outputs.script-file }}
      env:
        API_KEY: ${{ inputs.api-key }}
        INDEX: ${{ inputs.index }}
        VECTORS_FILE: ${{ inputs.vectors-file }}
