name: 'pleme-io · fastlane-deploy'
description: 'Run a fastlane lane to deploy iOS/Android build.'
branding: { icon: 'smartphone', color: 'red' }

inputs:
  lane:
    required: true
  working-directory:
    required: false
    default: "."

outputs:
  deployed:
    value: ${{ steps.run.outputs.deployed }}

runs:
  using: composite
  steps:
    - id: run
      shell: bash
      run: |
        echo "::warning::fastlane-deploy action stub — full implementation tracked in patterns.nix backlog"
        echo "deployed=stub" >> "$GITHUB_OUTPUT"
