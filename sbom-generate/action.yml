name: 'pleme-io · sbom-generate'
description: 'Generate a CycloneDX or SPDX SBOM from the repo via syft. Universal — works on any source tree (Rust, Node, Python, Helm, Docker context, etc).'
branding:
  icon: 'file-text'
  color: 'gray-dark'

inputs:
  output-format:
    description: "cyclonedx-json | spdx-json | syft-json"
    required: false
    default: "cyclonedx-json"
  output-file:
    description: "Path to write the SBOM"
    required: false
    default: "sbom.cyclonedx.json"

outputs:
  sbom-path:
    description: "Path of the written SBOM"
    value: ${{ steps.sbom.outputs.sbom-path }}
  component-count:
    description: "Number of components in the SBOM"
    value: ${{ steps.sbom.outputs.component-count }}

runs:
  using: composite
  steps:
    # Delegates to pleme-io/actions/install-syft so the pin + ARC-runner
    # correctness constraints live once, and so this action stays under the
    # NO-SHELL threshold the ★★ NO-SHELL directive gates on.
    - uses: pleme-io/actions/install-syft@main

    - name: Load stdlib + run.tlisp
      id: src
      shell: bash
      run: |
        set -euo pipefail
        out="${RUNNER_TEMP}/sbom-generate.tlisp"
        {
          # stdlib read in-checkout, at the SAME ref as the tlisp below -- never fetched.
          cat "${{ github.action_path }}/../_tlisp-stdlib/stdlib.tlisp"
          echo
          cat ${{ github.action_path }}/run.tlisp
        } > "$out"
        echo "script-file=$out" >> "$GITHUB_OUTPUT"

    - id: sbom
      uses: pleme-io/actions/tatara-script@main
      with:
        script-file: ${{ steps.src.outputs.script-file }}
      env:
        OUTPUT_FORMAT: ${{ inputs.output-format }}
        OUTPUT_FILE: ${{ inputs.output-file }}
