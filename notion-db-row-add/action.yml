name: 'pleme-io · notion-db-row-add'
description: 'Add a row to a Notion database.'
branding: { icon: 'database', color: 'gray-dark' }

inputs:
  token:
    required: true
  database-id:
    required: true
  properties-json:
    required: true

outputs:
  page-id:
    value: ${{ steps.run.outputs.page-id }}

runs:
  using: composite
  steps:
    - id: src
      shell: bash
      run: |
        set -euo pipefail
        out="${RUNNER_TEMP}/notion-db-row-add.tlisp"
        {
          # stdlib read in-checkout, at the SAME ref as the tlisp below -- never fetched.
          cat "${{ github.action_path }}/../_tlisp-stdlib/stdlib.tlisp"
          echo
          cat ${{ github.action_path }}/run.tlisp
        } > "$out"
        echo "script-file=$out" >> "$GITHUB_OUTPUT"
    - id: run
      uses: pleme-io/actions/tatara-script@main
      with:
        script-file: ${{ steps.src.outputs.script-file }}
      env:
        TOKEN: ${{ inputs.token }}
        DATABASE_ID: ${{ inputs.database-id }}
        PROPERTIES_JSON: ${{ inputs.properties-json }}
