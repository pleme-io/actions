# tlisp-lint/stdlib-shadow-baseline.txt
#
# Pre-existing stdlib SHADOWS, one "<path>\t<name>" row each.
#
# action.yml concatenates _tlisp-stdlib/stdlib.tlisp FIRST and an action's
# run.tlisp SECOND, and a later top-level (define …) WINS. So every name listed
# here is an action carrying its OWN private copy of a stdlib definition — the
# stdlib's version is loaded and then never reached. While the copies agree this
# is only duplication; the moment one is edited it is a silent divergence.
#
# It has already cost a release line. rust-workspace-publish carried its own
# dep-missing-on-line?, narrowed until it matched nothing cargo emits, while the
# stdlib's cargo-publish-dep-missing-line? matched the real log and was never
# called. A textbook sibling-ordering failure was classified "no recognizable
# signature" and the whole tatara-lisp 0.3.x line sat unpublished at 0.2.5.
#
# Every row is REPORTED on every lint run, never silently accepted. Only a NEW
# shadow fails the build. Removing a row — delete the local copy, use the
# stdlib's — is the tracked path to zero. Do not add a row to silence a fresh
# shadow you could simply delete instead.
#
# Counted 2026-07-29: 21 rows across 12 actions. rust-workspace-publish and
# tlisp-lint are deliberately absent — both were cleaned to zero when the
# gate landed.
#
# TIER + KNOWN LIMIT (do not round this up). The rule is CI-caught, not
# unrepresentable, and it detects only a SAME-NAME re-declaration. It does NOT
# catch an action re-implementing a stdlib predicate under a NEW name and using
# that instead — which is literally what happened: the local predicate was
# called `dep-missing-on-line?` while the stdlib's is
# `cargo-publish-dep-missing-line?`, so no name collided. (That file was still
# catchable here, because it ALSO re-declared `any-line-matches?`.) A
# phrase-level rule — "cargo error strings live only in the stdlib" — was
# measured and rejected: the literals are too generic (`"version"` alone hits 8
# actions), so it would have been a noisy gate, and a noisy gate gets deleted.
#
# UPDATE 2026-07-29 — the differently-named half is now COVERED, by LAYER 5
# (`tlisp-lint/stdlib-literal-baseline.txt`), and the phrase rule was NOT
# rejected outright but re-narrowed on three MEASURED axes until it was quiet:
# comments stripped (the biggest win — 6 of 14 raw rows were prose in
# rust-workspace-publish's own comments about this very bug), literals ≥16 chars
# containing a space, and no shell metacharacter (dropping the `>/dev/null 2>&1`
# family). `*.test.tlisp` is exempt, because a test file's job is to hold a
# foreign tool's verbatim output. Result: 4 baselined rows, 353 files clean.
#
# PROVEN against the exact historical shape: a file containing only
#   (define (dep-missing-on-line? line)
#     (and (string-contains? line "failed to select a version")
#          (string-contains? line "which is not on the registry")))
# passes Layers 1-4 (Layer 1 even logs `OK`) and is CAUGHT by Layer 5 —
# "failed to select a version" is a strict prefix of the stdlib's
# "failed to select a version for", which is why matching is CONTAINMENT in
# either direction rather than equality.
#
# STILL OPEN, stated so nobody reads the above as more than it is: Layer 5 is a
# CI-caught HEURISTIC over re-typed literals. A reimplementation that reassembles
# the match from short fragments, computes it, or duplicates a stdlib function
# with no long literal in it remains invisible to both layers. "The class is
# sealed" would be a round-up; "the historical instance and anything shaped like
# it are caught" is the honest claim.
ansible-collection-build/run.tlisp	append-output
cargo-publish-crate/run.tlisp	append-output
derive-version-from-tag/run.tlisp	append-output
detect-repo-type/run.tlisp	append-output
gem-publish/run.tlisp	extract-quoted-value
git-commit-tag/run.tlisp	append-output
helm-publish/run.tlisp	helm-published?
npm-publish/run.tlisp	npm-published?
python-publish/run.tlisp	pypi-published?
rust-workspace-bump/run.tlisp	append-output
rust-workspace-bump/run.tlisp	status-of
rust-workspace-bump/run.tlisp	stdout-of
rust-workspace-bump/run.tlisp	last-tag
spec-watch/run.tlisp	append-output
substrate-bump/run.tlisp	append-output
