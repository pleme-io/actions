name: 'pleme-io · sonarqube-scan'
description: 'Run SonarQube/SonarCloud scan + push results.'
branding: { icon: 'activity', color: 'blue' }

inputs:
  token:
    required: true
  project-key:
    required: true
  host-url:
    required: false
    default: "https://sonarcloud.io"

outputs:
  scan-id:
    value: ${{ steps.run.outputs.scan-id }}

runs:
  using: composite
  steps:
    - id: src
      shell: bash
      run: |
        set -euo pipefail
        out="${RUNNER_TEMP}/sonarqube-scan.tlisp"
        {
          # stdlib read in-checkout, at the SAME ref as the tlisp below -- never fetched.
          cat "${{ github.action_path }}/../_tlisp-stdlib/stdlib.tlisp"
          echo
          cat ${{ github.action_path }}/run.tlisp
        } > "$out"
        echo "script-file=$out" >> "$GITHUB_OUTPUT"
    - id: run
      uses: pleme-io/actions/tatara-script@main
      with:
        script-file: ${{ steps.src.outputs.script-file }}
      env:
        TOKEN: ${{ inputs.token }}
        PROJECT_KEY: ${{ inputs.project-key }}
        HOST_URL: ${{ inputs.host-url }}
