name: 'pleme-io · clickup-task-create'
description: 'Create a ClickUp task.'
branding: { icon: 'check-circle', color: 'purple' }

inputs:
  token:
    required: true
  list-id:
    required: true
  name:
    required: true

outputs:
  task-id:
    value: ${{ steps.run.outputs.task-id }}

runs:
  using: composite
  steps:
    - id: src
      shell: bash
      run: |
        set -euo pipefail
        out="${RUNNER_TEMP}/clickup-task-create.tlisp"
        {
          # stdlib read in-checkout, at the SAME ref as the tlisp below -- never fetched.
          cat "${{ github.action_path }}/../_tlisp-stdlib/stdlib.tlisp"
          echo
          cat ${{ github.action_path }}/run.tlisp
        } > "$out"
        echo "script-file=$out" >> "$GITHUB_OUTPUT"
    - id: run
      uses: pleme-io/actions/tatara-script@main
      with:
        script-file: ${{ steps.src.outputs.script-file }}
      env:
        TOKEN: ${{ inputs.token }}
        LIST_ID: ${{ inputs.list-id }}
        NAME: ${{ inputs.name }}
